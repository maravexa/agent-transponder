[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "agent-transponder-analysis"
version = "0.1.0"
description = "Agent Transponder Analysis Engine"
requires-python = ">=3.12"

[project.optional-dependencies]
test = ["pytest>=8"]
