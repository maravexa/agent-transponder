---
# ---------------------------------------------------------------------------
# node_exporter role defaults.
# Override in group_vars or host_vars as needed.
# ---------------------------------------------------------------------------

# Binary version and checksum — update together when upgrading.
# Checksum for node_exporter-<version>.linux-amd64.tar.gz from GitHub releases.
node_exporter_version: "1.8.1"
node_exporter_checksum: "sha256:FIXME_REPLACE_WITH_ACTUAL_CHECKSUM"

# Network binding.
node_exporter_listen_address: "0.0.0.0"
node_exporter_listen_port: 9100
