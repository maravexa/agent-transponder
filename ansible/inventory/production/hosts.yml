---
# Production inventory — fill in hostnames/IPs before use.
# IMPORTANT: Review hardening defaults and SSH keys before running against production.
all:
  children:
    monitoring:
      hosts:
        # prod-monitor-01:
        #   ansible_host: 10.1.0.10
        #   ansible_user: deploy
    flight_recorder:
      hosts:
        # prod-fr-01:
        #   ansible_host: 10.1.0.20
        #   ansible_user: deploy
        # prod-fr-02:
        #   ansible_host: 10.1.0.21
        #   ansible_user: deploy
