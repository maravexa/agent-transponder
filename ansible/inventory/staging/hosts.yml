---
# Staging inventory — fill in hostnames/IPs before use.
all:
  children:
    monitoring:
      hosts:
        # staging-monitor-01:
        #   ansible_host: 10.0.1.10
        #   ansible_user: deploy
    flight_recorder:
      hosts:
        # staging-fr-01:
        #   ansible_host: 10.0.1.20
        #   ansible_user: deploy
