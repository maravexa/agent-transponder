---
# One-command dev bootstrap. Targets localhost, hardening disabled by default.
- name: Local dev bootstrap
  hosts: localhost
  connection: local
  become: true
  vars:
    hardening_enabled: false
    tls_self_signed: true
    grafana_anonymous_auth: true
  roles:
    - common
    - hardening
    - tls
    - prometheus
    - grafana
    - node_exporter
    - ingester
    - analyzer
    - metrics_exporter
